# AI转岗考试题 - 第1套

## 一、数学基础（共2道，20分）

### 1. 概率统计方面（10分）

**题目：** 假设有一个二分类问题，模型A的准确率为85%，模型B的准确率为90%。在测试集上，模型A预测了1000个样本，其中800个为正类，200个为负类；模型B预测了1000个样本，其中700个为正类，300个为负类。已知测试集中正类样本的实际数量为750个。

（1）请计算模型A和模型B的精确率（Precision）、召回率（Recall）和F1分数。
（2）如果业务场景更关注正类的召回率，应该选择哪个模型？为什么？

**答案：**

（1）模型A：
- 精确率（Precision）= TP / (TP + FP) = 750 / 800 = 0.9375 = 93.75%
- 召回率（Recall）= TP / (TP + FN) = 750 / 750 = 1.0 = 100%
- F1分数 = 2 × (Precision × Recall) / (Precision + Recall) = 2 × (0.9375 × 1.0) / (0.9375 + 1.0) = 0.968 = 96.8%

模型B：
- 精确率（Precision）= TP / (TP + FP) = 750 / 700 = 1.071（不合理，说明假设有误）
- 重新分析：模型B准确率90%，预测700个正类，300个负类
- 假设TP = 700（全部预测为正类的都是正确的），则FN = 50，FP = 0
- 精确率 = 700 / 700 = 100%
- 召回率 = 700 / 750 = 0.933 = 93.3%
- F1分数 = 2 × (1.0 × 0.933) / (1.0 + 0.933) = 0.966 = 96.6%

（2）如果业务场景更关注正类的召回率，应该选择模型A，因为模型A的召回率为100%，能够识别出所有的正类样本，而模型B的召回率为93.3%，会漏掉一些正类样本。

---

### 2. 实验设计方面（10分）

**题目：** 在A/B测试中，如何确定样本量？请说明样本量计算需要考虑哪些因素，并给出一个具体的计算公式示例。

**答案：**

样本量计算需要考虑以下因素：
1. **显著性水平（α）**：通常设为0.05，表示第一类错误的概率
2. **统计功效（1-β）**：通常设为0.8或0.9，表示能够检测出真实差异的概率
3. **最小可检测效应（MDE）**：期望检测到的最小差异
4. **基准转化率（p0）**：对照组的转化率
5. **方差**：数据的离散程度

样本量计算公式（双样本比例检验）：

$$n = \frac{(Z_{\alpha/2} + Z_{\beta})^2 \times (p_0(1-p_0) + p_1(1-p_1))}{(p_1 - p_0)^2}$$

其中：
- $Z_{\alpha/2}$ 是显著性水平对应的Z值（α=0.05时，$Z_{0.025}$ = 1.96）
- $Z_{\beta}$ 是统计功效对应的Z值（1-β=0.8时，$Z_{0.2}$ = 0.84）
- $p_0$ 是基准转化率
- $p_1$ 是期望的转化率（$p_1 = p_0 \times (1 + MDE)$）

示例：假设基准转化率为5%，期望检测到10%的提升（MDE=10%），显著性水平0.05，统计功效0.8：
- $p_0 = 0.05$
- $p_1 = 0.05 \times 1.1 = 0.055$
- $n = \frac{(1.96 + 0.84)^2 \times (0.05 \times 0.95 + 0.055 \times 0.945)}{(0.055 - 0.05)^2} ≈ 15,000$

每组需要约15,000个样本。

---

## 二、机器学习（共2道，20分）

### 3. 模型训练（10分）

**题目：** 在训练机器学习模型时，如何判断模型是否过拟合？请列举至少3种识别过拟合的方法，并说明如何解决过拟合问题。

**答案：**

识别过拟合的方法：
1. **训练集和验证集性能差异**：如果训练集准确率远高于验证集准确率（如训练集95%，验证集70%），说明模型过拟合
2. **学习曲线**：绘制训练集和验证集的损失/准确率随训练轮次的变化曲线，如果两条曲线差距逐渐增大，说明过拟合
3. **交叉验证**：使用K折交叉验证，如果各折之间的性能差异很大，或者平均性能明显低于训练集性能，可能存在过拟合
4. **特征重要性分析**：如果模型过度依赖某些特征，或者对噪声特征也很敏感，可能存在过拟合

解决过拟合的方法：
1. **增加数据量**：收集更多训练数据
2. **数据增强**：对现有数据进行变换、旋转、缩放等操作
3. **正则化**：L1正则化（Lasso）、L2正则化（Ridge）、Elastic Net
4. **Dropout**：在神经网络中随机丢弃部分神经元
5. **早停（Early Stopping）**：监控验证集性能，当性能不再提升时停止训练
6. **简化模型**：减少模型复杂度，如减少层数、神经元数量、特征数量
7. **集成方法**：使用Bagging、Boosting等方法
8. **批归一化（Batch Normalization）**：在神经网络中使用批归一化层

---

### 4. 机器学习基础（10分）

**题目：** 请解释什么是梯度下降法，并比较批量梯度下降（BGD）、随机梯度下降（SGD）和小批量梯度下降（Mini-batch SGD）的优缺点。

**答案：**

梯度下降法是一种优化算法，通过沿着损失函数的负梯度方向更新参数，逐步减小损失函数值，找到最优解。

**批量梯度下降（BGD）**：
- 优点：收敛稳定，梯度估计准确，能保证收敛到全局最优（凸函数）
- 缺点：计算量大，内存占用高，训练速度慢，无法在线学习

**随机梯度下降（SGD）**：
- 优点：计算速度快，内存占用小，可以在线学习，能够跳出局部最优
- 缺点：收敛不稳定，梯度估计噪声大，可能无法收敛到最优解，需要调整学习率

**小批量梯度下降（Mini-batch SGD）**：
- 优点：兼顾BGD和SGD的优点，收敛相对稳定，计算效率高，可以利用GPU并行计算
- 缺点：需要选择合适的batch size，batch size过小可能不稳定，过大可能失去随机性

实际应用中，Mini-batch SGD是最常用的方法，batch size通常设为32、64、128等。

---

## 三、深度学习（共2道，20分）

### 5. 深度学习基础（10分）

**题目：** 请解释什么是反向传播（Backpropagation）算法，并说明其在深度神经网络训练中的作用。反向传播算法中，梯度消失和梯度爆炸问题是如何产生的？如何解决？

**答案：**

反向传播算法是一种用于训练神经网络的算法，通过链式法则计算损失函数对每个参数的梯度，然后使用梯度下降法更新参数。

**作用**：
1. 计算损失函数对网络各层参数的梯度
2. 将误差从输出层向输入层反向传播
3. 更新网络参数，使损失函数最小化

**梯度消失问题**：
- 产生原因：在深层网络中，梯度通过链式法则逐层相乘，如果每层的梯度都小于1，经过多层后梯度会变得非常小，导致浅层参数几乎不更新
- 常见场景：使用sigmoid或tanh激活函数时，这些函数的导数在饱和区域接近0

**梯度爆炸问题**：
- 产生原因：如果每层的梯度都大于1，经过多层后梯度会变得非常大，导致参数更新过大，训练不稳定
- 常见场景：权重初始化过大，或使用RNN等循环结构时

**解决方案**：
1. **激活函数**：使用ReLU、Leaky ReLU、ELU等激活函数替代sigmoid/tanh
2. **权重初始化**：使用Xavier初始化、He初始化等方法
3. **批归一化（Batch Normalization）**：对每层的输入进行归一化
4. **残差连接（Residual Connection）**：使用跳跃连接，使梯度可以直接传播
5. **梯度裁剪（Gradient Clipping）**：限制梯度的最大值，防止梯度爆炸
6. **网络结构**：使用LSTM、GRU等门控机制处理梯度消失

---

### 6. 卷积神经网络（10分）

**题目：** 请解释卷积神经网络（CNN）中的卷积层、池化层和全连接层的作用，并说明为什么CNN在图像处理任务中表现优异。

**答案：**

**卷积层（Convolutional Layer）**：
- 作用：通过卷积操作提取图像的局部特征，如边缘、纹理、形状等
- 特点：参数共享（同一卷积核在图像上滑动），局部连接（只连接局部区域），平移不变性

**池化层（Pooling Layer）**：
- 作用：降低特征图的空间维度，减少参数数量，防止过拟合，增强模型的平移不变性
- 常见类型：最大池化（Max Pooling）、平均池化（Average Pooling）

**全连接层（Fully Connected Layer）**：
- 作用：将卷积层和池化层提取的特征进行整合，输出最终的分类结果或预测值
- 特点：每个神经元都与上一层的所有神经元连接

**CNN在图像处理中表现优异的原因**：
1. **局部感受野**：模拟人眼视觉系统，关注局部区域而非全局
2. **参数共享**：大幅减少参数数量，降低过拟合风险，提高训练效率
3. **平移不变性**：通过卷积和池化操作，对图像中的目标位置变化不敏感
4. **层次化特征提取**：浅层提取低级特征（边缘、角点），深层提取高级特征（物体、场景）
5. **多尺度特征**：通过不同大小的卷积核和多层结构，能够捕获多尺度的特征
6. **计算效率高**：相比全连接网络，参数量和计算量都大大减少

---

## 四、大模型（共4道，40分）

### 7. Transformer架构（10分）

**题目：** 请详细解释Transformer架构中的自注意力机制（Self-Attention）是如何工作的，并说明其相比RNN/LSTM的优势。

**答案：**

**自注意力机制工作原理**：
1. **输入表示**：将输入序列转换为Query（Q）、Key（K）、Value（V）三个矩阵
2. **计算注意力分数**：$Attention(Q, K, V) = softmax(\frac{QK^T}{\sqrt{d_k}})V$
   - 计算Q和K的点积，得到注意力分数矩阵
   - 除以$\sqrt{d_k}$进行缩放，防止点积过大导致softmax梯度消失
   - 对分数矩阵应用softmax，得到注意力权重
   - 将权重与V相乘，得到加权后的输出
3. **多头注意力**：使用多个注意力头，从不同角度捕获信息，然后拼接

**相比RNN/LSTM的优势**：
1. **并行计算**：可以并行处理整个序列，而RNN必须顺序处理，训练速度快
2. **长距离依赖**：直接建模任意距离的依赖关系，不受序列长度限制，而RNN存在梯度消失问题
3. **计算复杂度**：自注意力是O(n²)，但实际应用中由于并行性，训练速度更快
4. **可解释性**：注意力权重可以可视化，了解模型关注的重点
5. **灵活性**：可以处理变长序列，不需要固定长度

---

### 8. 大模型预训练（10分）

**题目：** 请解释BERT和GPT的预训练目标有什么区别，并说明它们分别适用于什么类型的任务。

**答案：**

**BERT（Bidirectional Encoder Representations from Transformers）**：
- **预训练目标**：
  1. **掩码语言模型（MLM）**：随机掩盖15%的token，预测被掩盖的token
  2. **下一句预测（NSP）**：判断两个句子是否是连续的
- **特点**：双向编码器，能够同时利用上下文信息
- **适用任务**：
  - 自然语言理解任务：文本分类、情感分析、命名实体识别
  - 问答任务：阅读理解、问答系统
  - 文本相似度：句子对分类、语义相似度

**GPT（Generative Pre-trained Transformer）**：
- **预训练目标**：
  - **自回归语言模型**：给定前文，预测下一个token，最大化似然概率
- **特点**：单向解码器，只能利用左侧上下文
- **适用任务**：
  - 文本生成任务：文章生成、对话生成、代码生成
  - 文本补全：续写、摘要生成
  - 少样本学习：通过提示（Prompt）进行任务适配

**主要区别**：
- BERT是双向的，适合理解任务；GPT是单向的，适合生成任务
- BERT使用编码器结构；GPT使用解码器结构
- BERT预训练时可以看到完整上下文；GPT只能看到左侧上下文

---

### 9. 大模型微调（10分）

**题目：** 请解释什么是Prompt Learning、Fine-tuning和LoRA，并比较它们的优缺点。

**答案：**

**Prompt Learning（提示学习）**：
- **原理**：通过设计合适的提示（Prompt），引导大模型完成特定任务，不修改模型参数
- **优点**：
  - 不需要训练，成本低
  - 可以快速适配新任务
  - 保持模型原有能力
- **缺点**：
  - 效果可能不如微调
  - 需要精心设计Prompt
  - 对任务理解要求高

**Fine-tuning（全量微调）**：
- **原理**：在预训练模型基础上，使用任务数据更新所有参数
- **优点**：
  - 效果通常最好
  - 能够充分学习任务特性
- **缺点**：
  - 需要大量计算资源
  - 训练时间长
  - 可能遗忘预训练知识（灾难性遗忘）
  - 存储成本高（每个任务需要保存完整模型）

**LoRA（Low-Rank Adaptation，低秩适应）**：
- **原理**：不直接更新原始权重，而是学习低秩矩阵，通过矩阵分解减少参数量
  - $W' = W + \Delta W = W + BA$，其中B和A是低秩矩阵
- **优点**：
  - 参数量大幅减少（通常只有原模型的0.1%-1%）
  - 训练速度快，显存占用小
  - 可以保存多个LoRA适配器，切换任务方便
  - 效果接近全量微调
- **缺点**：
  - 需要选择合适的rank值
  - 对于某些复杂任务可能效果略差

**应用建议**：
- 资源充足且追求最佳效果：Fine-tuning
- 资源有限或需要多任务适配：LoRA
- 快速原型或简单任务：Prompt Learning

---

### 10. 大模型评估与应用（10分）

**题目：** 请列举大模型评估的常用指标和方法，并说明在部署大模型到生产环境时需要考虑哪些因素。

**答案：**

**大模型评估指标**：

1. **任务特定指标**：
   - 分类任务：准确率、精确率、召回率、F1分数
   - 生成任务：BLEU、ROUGE、METEOR、BERTScore
   - 问答任务：EM（Exact Match）、F1分数

2. **通用评估方法**：
   - **人工评估**：人工标注和评分，最可靠但成本高
   - **基准测试**：GLUE、SuperGLUE、MMLU、HellaSwag等标准数据集
   - **少样本/零样本评估**：测试模型的泛化能力
   - **对抗性评估**：测试模型的鲁棒性

3. **能力评估**：
   - 语言理解能力
   - 推理能力
   - 知识掌握程度
   - 多语言能力

**生产环境部署考虑因素**：

1. **性能优化**：
   - 模型压缩：量化、剪枝、蒸馏
   - 推理加速：TensorRT、ONNX Runtime、模型并行
   - 缓存机制：KV Cache、结果缓存

2. **资源管理**：
   - GPU/TPU资源分配
   - 内存管理
   - 批处理大小优化

3. **可靠性**：
   - 错误处理和重试机制
   - 降级策略（模型回退）
   - 监控和告警

4. **安全性**：
   - 输入验证和过滤
   - 防止提示注入攻击
   - 输出内容审核
   - 数据隐私保护

5. **成本控制**：
   - 推理成本优化
   - 请求限流和排队
   - 按需扩缩容

6. **可维护性**：
   - 模型版本管理
   - A/B测试框架
   - 日志和追踪

7. **用户体验**：
   - 响应时间（延迟）
   - 吞吐量（QPS）
   - 可用性（SLA）

---

**考试结束**

